<view wx:for="{{list}}" class="pd20 wux-light--bg mb1">
    <view class="">{{item.title}}</view>
    <view class="cold">{{item.content}}</view>
    <view class="wux-text--right fz24 cold">{{item.time}}</view>
</view>